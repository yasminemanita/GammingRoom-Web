{% extends 'base_clientSide.html.twig' %}

{% block title %}Reactionev{% endblock %}

{% block body %}
    <h1>Reactionev</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reactionev.id }}</td>
            </tr>
            <tr>
                <th>Interaction</th>
                <td>{{ reactionev.interaction }}</td>
            </tr>
            <tr>
                <th>Commentaire</th>
                <td>{{ reactionev.commentaire }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('reactionev_index') }}">back to list</a>

    <a href="{{ path('reactionev_edit', {'id': reactionev.id}) }}">edit</a>

    {{ include('reactionev/_delete_form.html.twig') }}
{% endblock %}
