<form method="post" action="{{ path('categori_membre_delete', {'id': categori_membre.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categori_membre.id) }}">
    <button class="btn">Delete</button>
</form>
