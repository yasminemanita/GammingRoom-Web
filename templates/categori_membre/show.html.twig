{% extends 'base.html.twig' %}

{% block title %}CategoriMembre{% endblock %}

{% block body %}
    <h1>CategoriMembre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categori_membre.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('categori_membre_index') }}">back to list</a>

    <a href="{{ path('categori_membre_edit', {'id': categori_membre.id}) }}">edit</a>

    {{ include('categori_membre/_delete_form.html.twig') }}
{% endblock %}
