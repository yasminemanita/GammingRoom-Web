{% extends 'base_admin.html.twig' %}

{% block stylesheets %}
  <style>
    .btn{
      width:200px;
    }

  </style>
{% endblock %}
{% block body %}
  <section class="content-header">
      <div class="container-fluid">
        <div class="row mb-2">
          <div class="col-sm-6">
            <h1>Jeux</h1>
          </div>
        </div>
      </div><!-- /.container-fluid -->
    </section>
 <!-- Main content -->
    <section class="content">
      <div class="container-fluid">
        <div class="row">
          <div class="col-12">
            <div class="card">
              <div class="card-header">
               
               </div>
              <!-- /.card-header -->
              <div class="card-body">
                <table id="example2" class="table table-bordered table-hover">
                  <thead>
                  <tr>
                    <th>Sujet</th>
                    <th>Contenue</th>
                    <th>Membre</th>
                    <th>Actions</th>
                  </tr>
                  </thead>
                  <tbody>
                  {% for re in rec %}
                  <tr>
                    <td>{{re.sujet}}</td>
                    <td>{{re.contenue}}</td>
                    <td>{{re.getMembre().nom}} {{re.getMembre().prenom}}</td>
                    <td>       
                    <a href="{{path('adminSupprimerReclamtion', {id: re.id})}}" > <button type="button" class="btn btn-danger   btn-lg" onclick="return confirm('Êtes-vous sûr?')">Supprimer</button></a>
                     </td>
                  </tr>
                  {% endfor %}
                  {% if rec|length ==0 %}
                  <tr>
                    <td colspan="10" style="text-align:center">il n'y a pas de réclamations</td>
                  </tr>
                  {% endif %}
                  </tbody>
                </table>
              </div>
              <!-- /.card-body -->
            </div>
            <!-- /.card -->
          </div>
          <!-- /.col -->
        </div>
        <!-- /.row -->
      </div>
      <!-- /.container-fluid -->
    </section>
    <!-- /.content -->
{% endblock %}
