<form method="post" action="{{ path('reactioncours_delete', {'id': reactioncour.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reactioncour.id) }}">
    <button class="btn">Delete</button>
</form>
